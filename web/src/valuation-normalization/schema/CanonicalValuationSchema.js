import { createOpaqueId } from '../../../../../packages/shared-types/src/index.js';
import { CANONICAL_VALUATION_SCHEMA_VERSION, VALUATION_NORMALIZATION_VERSION, ValuationResultStatus, assertRequired, assertNoInvestmentDecision } from '../contracts/index.js';
export class ValuationResultValidator { validate(r){ assertRequired(r,['valuationResultId','valuationArtifactId','normalizationVersion','canonicalSchemaVersion','companyId','securityId','currency','contributions','comparison','provenance','confidence','status','createdAt','extensions'],'canonicalValuationResult'); if(!Object.values(ValuationResultStatus).includes(r.status)) throw new Error(`Invalid valuation result status: ${r.status}`); if(!Array.isArray(r.contributions)) throw new Error('contributions must be an array'); assertNoInvestmentDecision(r,'Canonical valuation result'); return true; }}
export function createCanonicalValuationResult(input={}){ assertNoInvestmentDecision(input,'Canonical valuation result input'); const { valuationArtifactId, companyId, securityId, currency, contributions=[], comparison=[], provenance={}, confidence={}, extensions={} }=input; const result=deepFreeze({ valuationResultId:createOpaqueId('VRES'), valuationArtifactId, normalizationVersion:VALUATION_NORMALIZATION_VERSION, canonicalSchemaVersion:CANONICAL_VALUATION_SCHEMA_VERSION, companyId, securityId, currency, contributions:Object.freeze(contributions.slice()), comparison:Object.freeze(comparison.slice()), provenance:Object.freeze({ ...provenance }), confidence:Object.freeze({ ...confidence }), status:ValuationResultStatus.Active, createdAt:new Date().toISOString(), extensions:Object.freeze({ ...extensions }) }); new ValuationResultValidator().validate(result); return result; }
export const CanonicalValuationResult=Object.freeze({}); export const CanonicalValuationSchema=Object.freeze({ version:CANONICAL_VALUATION_SCHEMA_VERSION });
function deepFreeze(o){ Object.freeze(o); for(const v of Object.values(o)) if(v&&typeof v==='object'&&!Object.isFrozen(v)) deepFreeze(v); return o; }
