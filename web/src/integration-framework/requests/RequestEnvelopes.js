import { createOpaqueId } from '../../../../../packages/shared-types/src/index.js';
import { INTEGRATION_SCHEMA_VERSION, assertRequired, assertNoLiveIntegration } from '../contracts/index.js';
export class RequestEnvelopeValidator{ validate(r){ assertRequired(r,['requestEnvelopeId','integrationSchemaVersion','providerId','providerVersion','executionArtifactId','requestType','dryRun','payload','requestExtensions','createdAt'],'requestEnvelope'); if(r.dryRun!==true) throw new Error('dryRun must be true in Sprint 19'); for(const f of ['liveEndpoint','credentials','accountNumber','productionTransmission','routeVenue']) if(r[f]!==undefined) throw new Error(`Forbidden live request field: ${f}`); assertNoLiveIntegration(r,'request envelope'); return true; }}
export class RequestEnvelopeBuilder{ constructor(){ this.validator=new RequestEnvelopeValidator(); } build({ provider, executionArtifact, requestType='simulation-request', payload={}, dryRun=true, ...rest }){ const r=Object.freeze({ requestEnvelopeId:createOpaqueId('REQENV'), integrationSchemaVersion:INTEGRATION_SCHEMA_VERSION, providerId:provider.providerId, providerVersion:provider.providerVersion, executionArtifactId:executionArtifact.executionArtifactId, requestType, dryRun, payload:Object.freeze({ ...payload, instructions:executionArtifact.instructions||[] }), requestExtensions:{}, createdAt:new Date().toISOString(), ...rest }); this.validator.validate(r); return r; } broker(input){ return this.build({ ...input, requestType:'broker-dry-run' }); } exchange(input){ return this.build({ ...input, requestType:'exchange-dry-run' }); }}
export const BrokerRequestEnvelope=Object.freeze({}); export const ExchangeRequestEnvelope=Object.freeze({});
