import { SnapshotEngine } from '../../../../../packages/snapshot-engine/src/index.js';
import { INTEGRATION_SCHEMA_VERSION, assertNoLiveIntegration } from '../contracts/index.js';
export class IntegrationSnapshotManifestBuilder{ build({ artifact, auditTrail=[] }){ const m=Object.freeze({ integrationArtifactId:artifact.integrationArtifactId, integrationSchemaVersion:artifact.integrationSchemaVersion, providerId:artifact.providerId, providerVersion:artifact.providerVersion, executionArtifactId:artifact.requestEnvelope.executionArtifactId, requestEnvelope:artifact.requestEnvelope, simulationResult:artifact.simulationResult, providerCompatibility:artifact.providerCompatibility, auditTrail:Object.freeze(auditTrail.slice()), platformVersion:'2.0.0', snapshotSchemaVersion:INTEGRATION_SCHEMA_VERSION, createdAt:new Date().toISOString() }); assertNoLiveIntegration(m,'integration snapshot'); return m; }}
export class IntegrationSnapshotAdapter{ constructor({ snapshotStore }={}){ this.snapshotStore=snapshotStore; this.builder=new IntegrationSnapshotManifestBuilder(); } create(input){ const integrationManifest=this.builder.build(input); const snapshot=new SnapshotEngine().createSnapshot({ subject:{ type:'integration-artifact', id:integrationManifest.integrationArtifactId }, methodology:{ methodology_id:'INTEGRATION_FRAMEWORK', version:INTEGRATION_SCHEMA_VERSION }, data_version:INTEGRATION_SCHEMA_VERSION, platform_version:'2.0.0', inputs:{ integrationManifest }, outputs:{ simulationStatus:integrationManifest.simulationResult.acknowledgement.status } }); this.snapshotStore?.append?.(snapshot); return Object.freeze({ integrationManifest, snapshot }); }}
