import { createOpaqueId } from '../../../../../packages/shared-types/src/index.js';
import { RECOMMENDATION_SCHEMA_VERSION, RecommendationArtifactStatus, RecommendationDirection, assertRequired, assertNoDecisionLogic } from '../contracts/index.js';
export class RecommendationArtifactValidator { validate(a){ assertRequired(a,['recommendationArtifactId','recommendationSchemaVersion','recommendationModelId','recommendationModelVersion','inputPackageId','recommendationDirection','rationale','confidence','provenance','status','createdAt','extensions'],'recommendationArtifact'); if(!Object.values(RecommendationDirection).includes(a.recommendationDirection)) throw new Error(`Invalid recommendation direction: ${a.recommendationDirection}`); if(!Object.values(RecommendationArtifactStatus).includes(a.status)) throw new Error(`Invalid recommendation status: ${a.status}`); assertNoDecisionLogic(a,'recommendation artifact'); return true; }}
export function createRecommendationArtifact(input={}){ assertNoDecisionLogic(input,'recommendation artifact input'); const { recommendationModelId, recommendationModelVersion, inputPackageId, recommendationDirection, rationale, confidence={}, provenance={}, auditReference=null, status=RecommendationArtifactStatus.Generated, extensions={} }=input; const artifact=deepFreeze({ recommendationArtifactId:createOpaqueId('RECART'), recommendationSchemaVersion:RECOMMENDATION_SCHEMA_VERSION, recommendationModelId, recommendationModelVersion, inputPackageId, recommendationDirection, rationale, confidence:Object.freeze({ ...confidence }), provenance:Object.freeze({ ...provenance }), auditReference, status, createdAt:new Date().toISOString(), extensions:Object.freeze({ ...extensions }) }); new RecommendationArtifactValidator().validate(artifact); return artifact; }
export const RecommendationArtifactSchema=Object.freeze({ version:RECOMMENDATION_SCHEMA_VERSION });
function deepFreeze(o){ Object.freeze(o); for(const v of Object.values(o)) if(v&&typeof v==='object'&&!Object.isFrozen(v)) deepFreeze(v); return o; }
