import { createOpaqueId } from '../../../../../packages/shared-types/src/index.js';
import { assertNoDecisionLogic } from '../contracts/index.js';
export class RecommendationConfigurationRegistry { constructor(){ this.items=new Map(); } register({ recommendationModelId, recommendationModelVersion, configurationVersion='1.0.0', policySettings={} }){ const c=deepFreeze({ recommendationConfigurationId:createOpaqueId('RCFG'), recommendationModelId, recommendationModelVersion, configurationVersion, policySettings, createdAt:new Date().toISOString() }); for (const forbidden of ['orderGeneration','tradeExecution','portfolioConstruction','capitalAllocation','decisionApproval']) if (c.policySettings?.[forbidden] !== undefined) throw new Error(`Forbidden recommendation configuration field: ${forbidden}`); assertNoDecisionLogic(c,'recommendation configuration'); const key=`${recommendationModelId}@${recommendationModelVersion}:${configurationVersion}`; if(this.items.has(key)) throw new Error(`Recommendation configuration exists: ${key}`); this.items.set(key,c); return c; }}
export const RecommendationConfiguration=Object.freeze({}); export const RecommendationConfigurationValidator=Object.freeze({});
function deepFreeze(o){ Object.freeze(o); for(const v of Object.values(o)) if(v&&typeof v==='object'&&!Object.isFrozen(v)) deepFreeze(v); return o; }
