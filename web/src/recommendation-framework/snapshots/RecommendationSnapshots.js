import { SnapshotEngine } from '../../../../../packages/snapshot-engine/src/index.js';
import { RECOMMENDATION_SCHEMA_VERSION, assertNoDecisionLogic } from '../contracts/index.js';
export class RecommendationSnapshotManifestBuilder { build({ artifact, auditTrail=[] }){ const m=Object.freeze({ recommendationArtifactId:artifact.recommendationArtifactId, recommendationSchemaVersion:artifact.recommendationSchemaVersion, recommendationModelId:artifact.recommendationModelId, recommendationModelVersion:artifact.recommendationModelVersion, inputPackageId:artifact.inputPackageId, recommendationDirection:artifact.recommendationDirection, rationale:artifact.rationale, confidence:artifact.confidence, provenance:artifact.provenance, auditTrail:Object.freeze(auditTrail.slice()), platformVersion:'2.0.0', snapshotSchemaVersion:RECOMMENDATION_SCHEMA_VERSION, createdAt:new Date().toISOString() }); assertNoDecisionLogic(m,'recommendation snapshot'); return m; }}
export class RecommendationSnapshotAdapter { constructor({ snapshotStore }={}){ this.snapshotStore=snapshotStore; this.builder=new RecommendationSnapshotManifestBuilder(); } create(input){ const recommendationManifest=this.builder.build(input); const snapshot=new SnapshotEngine().createSnapshot({ subject:{ type:'recommendation-artifact', id:recommendationManifest.recommendationArtifactId }, methodology:{ methodology_id:recommendationManifest.recommendationModelId, version:recommendationManifest.recommendationModelVersion }, data_version:RECOMMENDATION_SCHEMA_VERSION, platform_version:'2.0.0', inputs:{ recommendationManifest }, outputs:{ recommendationDirection:recommendationManifest.recommendationDirection } }); this.snapshotStore?.append?.(snapshot); return Object.freeze({ recommendationManifest, snapshot }); }}
