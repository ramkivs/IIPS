export class RecommendationTelemetry { constructor(){ this.metrics={ recommendationModelsRegistered:0, inputPackagesCreated:0, policiesEvaluated:0, rationalesCreated:0, recommendationArtifactsCreated:0, snapshotsCreated:0, replaysCompleted:0, forbiddenActionAttempts:0 }; } increment(metric){ if(!(metric in this.metrics)) throw new Error(`Unknown recommendation metric: ${metric}`); this.metrics[metric]+=1; return this.snapshot(); } snapshot(){ return Object.freeze({ ...this.metrics }); }}
export class RecommendationHealthReporter { constructor({ telemetry }){ this.telemetry=telemetry; } status(){ const m=this.telemetry.snapshot(); return Object.freeze({ status:m.forbiddenActionAttempts?'Degraded':'Healthy', metrics:m }); }}
export class RecommendationDiagnostics { constructor({ diagnostics, telemetry=new RecommendationTelemetry() }={}){ this.diagnostics=diagnostics; this.telemetry=telemetry; this.healthReporter=new RecommendationHealthReporter({ telemetry }); } record(metric, meta={}){ this.telemetry.increment(metric); this.diagnostics?.record?.(`recommendation.${metric}`, meta); return this.telemetry.snapshot(); } health(){ return this.healthReporter.status(); }}
