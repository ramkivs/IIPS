export class PortfolioTelemetry{ constructor(){ this.metrics={ portfoliosRegistered:0, holdingsRegistered:0, allocationsCreated:0, intentsCreated:0, constraintsEvaluated:0, exposuresCalculated:0, changeProposalsCreated:0, snapshotsCreated:0, replaysCompleted:0, forbiddenActionAttempts:0 }; } increment(metric){ if(!(metric in this.metrics)) throw new Error(`Unknown portfolio metric: ${metric}`); this.metrics[metric]+=1; return this.snapshot(); } snapshot(){ return Object.freeze({ ...this.metrics }); }}
export class PortfolioHealthReporter{ constructor({ telemetry }){ this.telemetry=telemetry; } status(){ const m=this.telemetry.snapshot(); return Object.freeze({ status:m.forbiddenActionAttempts?'Degraded':'Healthy', metrics:m }); }}
export class PortfolioDiagnostics{ constructor({ diagnostics, telemetry=new PortfolioTelemetry() }={}){ this.diagnostics=diagnostics; this.telemetry=telemetry; this.healthReporter=new PortfolioHealthReporter({ telemetry }); } record(metric, meta={}){ this.telemetry.increment(metric); this.diagnostics?.record?.(`portfolio.${metric}`, meta); return this.telemetry.snapshot(); } health(){ return this.healthReporter.status(); }}
