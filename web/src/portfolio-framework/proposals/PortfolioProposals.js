import { createOpaqueId } from '../../../../../packages/shared-types/src/index.js';
import { PORTFOLIO_SCHEMA_VERSION, PortfolioChangeProposalStatus, assertRequired, assertNoExecutionLogic } from '../contracts/index.js';
export class PortfolioChangeProposalValidator{ validate(p){ assertRequired(p,['proposalId','proposalSchemaVersion','proposalModelId','proposalVersion','portfolioId','sourceDecisionArtifactId','proposedChanges','status','createdAt'],'portfolioChangeProposal'); for(const change of p.proposedChanges) for(const f of ['orderType','broker','venue','executionAlgo','routingInstruction','tradeInstruction','settlementInstruction']) if(change[f]!==undefined) throw new Error(`Forbidden proposal field: ${f}`); assertNoExecutionLogic(p,'portfolio change proposal'); return true; }}
export class PortfolioChangeProposalRegistry{ constructor(){ this.items=[]; this.validator=new PortfolioChangeProposalValidator(); } create(input){ const p=deepFreeze({ proposalId:createOpaqueId('PPROP'), proposalSchemaVersion:PORTFOLIO_SCHEMA_VERSION, proposalModelId:'PORTFOLIO_PROPOSAL_MODEL_1.0', proposalVersion:'1.0.0', status:PortfolioChangeProposalStatus.Proposed, createdAt:new Date().toISOString(), ...input }); this.validator.validate(p); this.items.push(p); return p; }}
export const PortfolioChangeProposal=Object.freeze({});
function deepFreeze(o){ Object.freeze(o); for(const v of Object.values(o)) if(v&&typeof v==='object'&&!Object.isFrozen(v)) deepFreeze(v); return o; }
