import { SnapshotEngine } from '../../../../../packages/snapshot-engine/src/index.js';
import { PORTFOLIO_SCHEMA_VERSION, assertNoExecutionLogic } from '../contracts/index.js';
export class PortfolioSnapshotManifestBuilder{ build({ portfolio, holdings=[], allocations=[], intents=[], constraints=[], exposures=[], composition=null, changeProposals=[], auditTrail=[] }){ const m=Object.freeze({ portfolioId:portfolio.portfolioId, portfolioVersion:portfolio.portfolioVersion, portfolioSchemaVersion:portfolio.portfolioSchemaVersion, holdings:Object.freeze(holdings.slice()), allocations:Object.freeze(allocations.slice()), intents:Object.freeze(intents.slice()), constraints:Object.freeze(constraints.slice()), exposures:Object.freeze(exposures.slice()), composition, changeProposals:Object.freeze(changeProposals.slice()), auditTrail:Object.freeze(auditTrail.slice()), platformVersion:'2.0.0', snapshotSchemaVersion:PORTFOLIO_SCHEMA_VERSION, createdAt:new Date().toISOString() }); assertNoExecutionLogic(m,'portfolio snapshot'); return m; }}
export class PortfolioSnapshotAdapter{ constructor({ snapshotStore }={}){ this.snapshotStore=snapshotStore; this.builder=new PortfolioSnapshotManifestBuilder(); } create(input){ const portfolioManifest=this.builder.build(input); const snapshot=new SnapshotEngine().createSnapshot({ subject:{ type:'portfolio', id:portfolioManifest.portfolioId }, methodology:{ methodology_id:'PORTFOLIO_FRAMEWORK', version:PORTFOLIO_SCHEMA_VERSION }, data_version:PORTFOLIO_SCHEMA_VERSION, platform_version:'2.0.0', inputs:{ portfolioManifest }, outputs:{ holdings:portfolioManifest.holdings.length, proposals:portfolioManifest.changeProposals.length } }); this.snapshotStore?.append?.(snapshot); return Object.freeze({ portfolioManifest, snapshot }); }}
