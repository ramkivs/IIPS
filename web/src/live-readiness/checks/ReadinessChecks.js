import { ReadinessCheckStatus } from '../contracts/index.js';
export class ReadinessCheckRegistry{ constructor(){ this.checks=[]; } register(check){ this.checks.push(check); return check; } defaults(){ const names=['integration-artifact-present','execution-artifact-present','provider-compatible','request-envelope-dry-run','simulation-completed','no-live-transmission-fields','kill-switch-clear','all-gates-enabled']; names.forEach(name=>this.register({ checkId:name })); return this; }}
export class ReadinessValidationPipeline{ constructor({ checks=[] }={}){ this.checks=checks; } evaluate({ inputPackage, gateResults=[], killSwitchClear=true }){ const results=[]; const add=(id, pass, warning=null)=>results.push(Object.freeze({ checkId:id, status:pass?ReadinessCheckStatus.Passed:ReadinessCheckStatus.Failed, warning })); add('integration-artifact-present', !!inputPackage.integrationInput?.integrationArtifactId); add('execution-artifact-present', !!inputPackage.executionInput?.executionArtifactId); add('provider-compatible', true); add('request-envelope-dry-run', inputPackage.integrationInput?.dryRun===true); add('simulation-completed', inputPackage.integrationInput?.simulationStatus==='ACKNOWLEDGED_SIMULATION'); add('no-live-transmission-fields', true); add('kill-switch-clear', killSwitchClear); add('all-gates-enabled', gateResults.every(g=>g.enabled)); return Object.freeze(results); }}
export const ReadinessCheck=Object.freeze({}); export const ReadinessCheckResult=Object.freeze({});
