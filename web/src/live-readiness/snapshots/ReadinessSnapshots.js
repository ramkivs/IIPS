import { SnapshotEngine } from '../../../../../packages/snapshot-engine/src/index.js';
import { LIVE_ENABLEMENT_SCHEMA_VERSION, assertNoLiveReadinessLeak } from '../contracts/index.js';
export class ReadinessSnapshotManifestBuilder{ build({ readiness, gateResults=[], authorizationRecords=[], killSwitchState=[], auditTrail=[] }){ const m=Object.freeze({ readinessId:readiness.readinessId, schemaVersion:readiness.schemaVersion, integrationArtifactId:readiness.integrationArtifactId, executionArtifactId:readiness.executionArtifactId, eligible:readiness.eligible, reasons:readiness.reasons, failedChecks:readiness.failedChecks, warnings:readiness.warnings, gateResults:Object.freeze(gateResults.slice()), authorizationRecords:Object.freeze(authorizationRecords.slice()), killSwitchState:Object.freeze(killSwitchState.slice()), auditTrail:Object.freeze(auditTrail.slice()), platformVersion:'2.0.0', snapshotSchemaVersion:LIVE_ENABLEMENT_SCHEMA_VERSION, createdAt:new Date().toISOString() }); assertNoLiveReadinessLeak(m,'readiness snapshot'); return m; }}
export class ReadinessSnapshotAdapter{ constructor({ snapshotStore }={}){ this.snapshotStore=snapshotStore; this.builder=new ReadinessSnapshotManifestBuilder(); } create(input){ const readinessManifest=this.builder.build(input); const snapshot=new SnapshotEngine().createSnapshot({ subject:{ type:'transmission-readiness', id:readinessManifest.readinessId }, methodology:{ methodology_id:'LIVE_READINESS', version:LIVE_ENABLEMENT_SCHEMA_VERSION }, data_version:LIVE_ENABLEMENT_SCHEMA_VERSION, platform_version:'2.0.0', inputs:{ readinessManifest }, outputs:{ eligible:readinessManifest.eligible } }); this.snapshotStore?.append?.(snapshot); return Object.freeze({ readinessManifest, snapshot }); }}
