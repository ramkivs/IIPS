export class ReadinessTelemetry{ constructor(){ this.metrics={ readinessEvaluationsStarted:0, readinessEvaluationsCompleted:0, readinessChecksFailed:0, approvalsCreated:0, killSwitchesActivated:0, snapshotsCreated:0, replaysCompleted:0, forbiddenActionAttempts:0 }; } increment(metric){ if(!(metric in this.metrics)) throw new Error(`Unknown readiness metric: ${metric}`); this.metrics[metric]+=1; return this.snapshot(); } snapshot(){ return Object.freeze({ ...this.metrics }); }}
export class ReadinessHealthReporter{ constructor({ telemetry }){ this.telemetry=telemetry; } status(){ const m=this.telemetry.snapshot(); return Object.freeze({ status:m.forbiddenActionAttempts?'Degraded':'Healthy', metrics:m }); }}
export class ReadinessDiagnostics{ constructor({ diagnostics, telemetry=new ReadinessTelemetry() }={}){ this.diagnostics=diagnostics; this.telemetry=telemetry; this.healthReporter=new ReadinessHealthReporter({ telemetry }); } record(metric, meta={}){ this.telemetry.increment(metric); this.diagnostics?.record?.(`readiness.${metric}`, meta); return this.telemetry.snapshot(); } health(){ return this.healthReporter.status(); }}
