import { createOpaqueId } from '../../../../../packages/shared-types/src/index.js';
import { SCORING_SCHEMA_VERSION, assertRequired, assertNoRecommendationLogic } from '../contracts/index.js';
export class ScoreArtifactValidator { validate(a){ assertRequired(a,['scoreArtifactId','scoreSchemaVersion','scoringModelId','scoringModelVersion','inputPackageId','components','aggregation','confidence','provenance','createdAt'], 'scoreArtifact'); if(!Array.isArray(a.components)) throw new Error('components must be an array'); assertNoRecommendationLogic(a,'score artifact'); return true; }}
export function createScoreArtifact(input={}){ assertNoRecommendationLogic(input,'score artifact input'); const { scoringModelId, scoringModelVersion, inputPackageId, components=[], aggregation, confidence={}, provenance={}, auditReference=null }=input; const artifact=deepFreeze({ scoreArtifactId:createOpaqueId('SART'), scoreSchemaVersion:SCORING_SCHEMA_VERSION, scoringModelId, scoringModelVersion, inputPackageId, components:Object.freeze(components.slice()), aggregation, confidence:Object.freeze({ ...confidence }), provenance:Object.freeze({ ...provenance }), auditReference, createdAt:new Date().toISOString() }); new ScoreArtifactValidator().validate(artifact); return artifact; }
export const ScoreArtifactSchema=Object.freeze({ version:SCORING_SCHEMA_VERSION });
function deepFreeze(o){ Object.freeze(o); for(const v of Object.values(o)) if(v&&typeof v==='object'&&!Object.isFrozen(v)) deepFreeze(v); return o; }
