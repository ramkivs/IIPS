export class ScoringTelemetry { constructor(){ this.metrics={ scoringModelsRegistered:0, inputPackagesCreated:0, componentsCreated:0, aggregationsCompleted:0, scoreArtifactsCreated:0, snapshotsCreated:0, replaysCompleted:0, forbiddenActionAttempts:0 }; } increment(metric){ if(!(metric in this.metrics)) throw new Error(`Unknown scoring metric: ${metric}`); this.metrics[metric]+=1; return this.snapshot(); } snapshot(){ return Object.freeze({ ...this.metrics }); }}
export class ScoringHealthReporter { constructor({ telemetry }){ this.telemetry=telemetry; } status(){ const m=this.telemetry.snapshot(); return Object.freeze({ status:m.forbiddenActionAttempts?'Degraded':'Healthy', metrics:m }); }}
export class ScoringDiagnostics { constructor({ diagnostics, telemetry=new ScoringTelemetry() }={}){ this.diagnostics=diagnostics; this.telemetry=telemetry; this.healthReporter=new ScoringHealthReporter({ telemetry }); } record(metric, meta={}){ this.telemetry.increment(metric); this.diagnostics?.record?.(`scoring.${metric}`, meta); return this.telemetry.snapshot(); } health(){ return this.healthReporter.status(); }}
