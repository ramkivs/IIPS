import { MethodologyManifestValidator } from '../definitions/index.js';
export class MethodologyFrameworkRegistryAdapter { constructor({ methodologyRegistry, diagnostics }={}){ this.methodologyRegistry=methodologyRegistry; this.diagnostics=diagnostics; this.validator=new MethodologyManifestValidator(); }
  sync(manifest){ this.validator.validate(manifest); const record=this.methodologyRegistry.register({ methodology_id:manifest.methodologyId, version:manifest.methodologyVersion, owner:manifest.owner, status:manifest.status==='active'?'active':'draft', effective_date:new Date().toISOString(), inputs:manifest.evidenceRequirements, outputs:manifest.outputContract, calibration_reference:null, backtest_reference:null, description:manifest.description }); this.diagnostics?.record?.('methodology.registry.synced',{ methodologyId:manifest.methodologyId }); return record; }}
export class MethodologyRegistrySynchronizer { constructor({ adapter }){ this.adapter=adapter; } sync(manifest){ return this.adapter.sync(manifest); }}
export class MethodologyRegistryDiagnostics { constructor({ registry }){ this.registry=registry; } snapshot(){ return Object.freeze({ count:this.registry?.list?.().length||0 }); }}
