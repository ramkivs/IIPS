import { EvidenceCertificationStatus } from '../../evidence/index.js';
import { MethodologyInputType, assertNoInvestmentAnalysis } from '../contracts/index.js';
export class MethodologyEvidenceRequirement { constructor({ requirementId, methodologyId, methodologyVersion, evidenceType='research-source', required=true, minCount=1, acceptedCertificationStatuses=[EvidenceCertificationStatus.Certified], acceptedSourceTypes=[], description='' }){ const r=Object.freeze({ requirementId, methodologyId, methodologyVersion, evidenceType, required, minCount, acceptedCertificationStatuses:Object.freeze(acceptedCertificationStatuses.slice()), acceptedSourceTypes:Object.freeze(acceptedSourceTypes.slice()), description }); assertNoInvestmentAnalysis(r,'Evidence requirement'); return r; }}
export class EvidenceRequirementEngine { constructor(){ this.requirements=[]; } declare(input){ const r=new MethodologyEvidenceRequirement(input); this.requirements.push(r); return r; } byMethodology(id,version){ return Object.freeze(this.requirements.filter(r=>r.methodologyId===id&&r.methodologyVersion===version)); }}
export class CertifiedEvidenceInputAdapter { adapt(evidence){ if(evidence.certificationStatus!==EvidenceCertificationStatus.Certified) throw new Error('Certified evidence is required'); return Object.freeze({ inputType:MethodologyInputType.certifiedEvidence, evidenceRecordId:evidence.evidenceRecordId, evidenceVersionId:evidence.evidenceVersionId, evidenceType:evidence.evidenceType, sourceType:evidence.provenance?.sourceType, certificationStatus:evidence.certificationStatus }); } rejectRawResearchArtifact(){ throw new Error('Raw research artifacts are not methodology inputs'); }}
export class EvidenceSufficiencyChecker { check({ requirements, evidenceInputs }){ const missing=[]; for(const r of requirements){ const matches=evidenceInputs.filter(e=>e.evidenceType===r.evidenceType && r.acceptedCertificationStatuses.includes(e.certificationStatus)); if(r.required && matches.length<r.minCount) missing.push(r.requirementId); } return Object.freeze({ passed:missing.length===0, missing:Object.freeze(missing), checkedAt:new Date().toISOString() }); }}
export const EvidenceInputContract = Object.freeze({});
