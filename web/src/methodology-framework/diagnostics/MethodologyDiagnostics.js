export class MethodologyTelemetry { constructor(){ this.metrics={ methodologiesRegistered:0, methodologiesActivated:0, configurationsRegistered:0, evidenceRequirementsDeclared:0, dryRunsExecuted:0, validationsExecuted:0, replaysExecuted:0, snapshotsCreated:0, forbiddenActionAttempts:0 }; } increment(metric){ if(!(metric in this.metrics)) throw new Error(`Unknown methodology metric: ${metric}`); this.metrics[metric]+=1; return this.snapshot(); } snapshot(){ return Object.freeze({ ...this.metrics }); }}
export class MethodologyHealthReporter { constructor({ telemetry }){ this.telemetry=telemetry; } status(){ const m=this.telemetry.snapshot(); return Object.freeze({ status:m.forbiddenActionAttempts>0?'Degraded':'Healthy', metrics:m }); }}
export class MethodologyDiagnostics { constructor({ diagnostics, telemetry=new MethodologyTelemetry() }={}){ this.diagnostics=diagnostics; this.telemetry=telemetry; this.healthReporter=new MethodologyHealthReporter({ telemetry }); } record(metric, meta={}){ const normalized=normalizeMetric(metric); this.telemetry.increment(normalized); this.diagnostics?.record?.(`methodologyFramework.${normalized}`, meta); return this.telemetry.snapshot(); } health(){ return this.healthReporter.status(); }}
function normalizeMetric(metric){ return ({ 'methodology.definition.registered':'methodologiesRegistered', 'methodology.registry.synced':'methodologiesRegistered', 'methodology.lifecycle.failed':'forbiddenActionAttempts' })[metric] || metric; }
