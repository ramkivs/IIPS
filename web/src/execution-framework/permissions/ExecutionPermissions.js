import { ExecutionPermissionAction } from '../contracts/index.js';
const forbidden=new Set(['brokerIntegration','placeOrder','executeTrade','connectExchange','settleTrade','transmitOrder']);
export class ExecutionPermissionRegistry{ constructor(){ this.permissions=new Map(); } register({ action, effect='allow' }){ if(forbidden.has(action)) throw new Error(`Forbidden execution permission: ${action}`); if(!Object.values(ExecutionPermissionAction).includes(action)) throw new Error(`Unsupported execution permission: ${action}`); if(this.permissions.has(action)) throw new Error(`Permission exists: ${action}`); const p=Object.freeze({ action, effect }); this.permissions.set(action,p); return p; } get(action){ return this.permissions.get(action)||null; }}
export class ExecutionPermissionGate{ constructor({ registry }){ this.registry=registry; } require(action){ const p=this.registry.get(action); if(!p||p.effect!=='allow') throw new Error(`Execution permission denied: ${action}`); return true; }}
export function registerDefaultExecutionPermissions(registry){ for(const action of Object.values(ExecutionPermissionAction)) if(!registry.get(action)) registry.register({ action }); return registry; }
