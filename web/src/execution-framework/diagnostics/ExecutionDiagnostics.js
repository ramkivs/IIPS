export class ExecutionTelemetry{ constructor(){ this.metrics={ executionIntentsCreated:0, executionPlansCreated:0, executionPoliciesEvaluated:0, executionInstructionsCreated:0, executionArtifactsCreated:0, snapshotsCreated:0, replaysCompleted:0, forbiddenActionAttempts:0 }; } increment(metric){ if(!(metric in this.metrics)) throw new Error(`Unknown execution metric: ${metric}`); this.metrics[metric]+=1; return this.snapshot(); } snapshot(){ return Object.freeze({ ...this.metrics }); }}
export class ExecutionHealthReporter{ constructor({ telemetry }){ this.telemetry=telemetry; } status(){ const m=this.telemetry.snapshot(); return Object.freeze({ status:m.forbiddenActionAttempts?'Degraded':'Healthy', metrics:m }); }}
export class ExecutionDiagnostics{ constructor({ diagnostics, telemetry=new ExecutionTelemetry() }={}){ this.diagnostics=diagnostics; this.telemetry=telemetry; this.healthReporter=new ExecutionHealthReporter({ telemetry }); } record(metric, meta={}){ this.telemetry.increment(metric); this.diagnostics?.record?.(`execution.${metric}`, meta); return this.telemetry.snapshot(); } health(){ return this.healthReporter.status(); }}
