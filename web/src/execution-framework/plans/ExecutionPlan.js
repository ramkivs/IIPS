import { createOpaqueId } from '../../../../../packages/shared-types/src/index.js';
import { EXECUTION_SCHEMA_VERSION, ExecutionPlanStatus, ExecutionStrategyType, assertRequired, assertNoExternalExecution } from '../contracts/index.js';
export class ExecutionPlanValidator{ validate(p){ assertRequired(p,['executionPlanId','executionPlanVersion','executionSchemaVersion','executionArtifactId','executionIntentId','portfolioChangeProposalId','strategyType','routePlanPlaceholder','instructions','status','createdAt'],'executionPlan'); if(!Object.values(ExecutionStrategyType).includes(p.strategyType)) throw new Error('Invalid strategy type'); assertNoExternalExecution(p,'execution plan'); return true; }}
export class ExecutionPlanRegistry{ constructor(){ this.items=[]; this.validator=new ExecutionPlanValidator(); } create(input){ const plan=deepFreeze({ executionPlanId:createOpaqueId('EPLAN'), executionPlanVersion:'1.0.0', executionSchemaVersion:EXECUTION_SCHEMA_VERSION, executionArtifactId:createOpaqueId('EART'), status:ExecutionPlanStatus.Planned, instructions:[], createdAt:new Date().toISOString(), ...input }); this.validator.validate(plan); this.items.push(plan); return plan; }}
export const ExecutionPlan=Object.freeze({});
function deepFreeze(o){ Object.freeze(o); for(const v of Object.values(o)) if(v&&typeof v==='object'&&!Object.isFrozen(v)) deepFreeze(v); return o; }
