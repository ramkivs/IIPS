export class CompanySecurityTelemetry { constructor(){ this.metrics={ companiesRegistered:0, securitiesRegistered:0, exchangesRegistered:0, listingsRegistered:0, identifiersMapped:0, classificationsAssigned:0, corporateActionsRecorded:0, validationFailures:0, forbiddenActionAttempts:0 }; } increment(metric){ if(!(metric in this.metrics)) throw new Error(`Unknown domain metric: ${metric}`); this.metrics[metric]+=1; return this.snapshot(); } snapshot(){ return Object.freeze({ ...this.metrics }); }}
export class CompanySecurityHealthReporter { constructor({ telemetry }){ this.telemetry=telemetry; } status(){ const m=this.telemetry.snapshot(); return Object.freeze({ status:m.validationFailures||m.forbiddenActionAttempts?'Degraded':'Healthy', metrics:m }); }}
export class CompanySecurityDiagnostics { constructor({ diagnostics, telemetry=new CompanySecurityTelemetry() }={}){ this.diagnostics=diagnostics; this.telemetry=telemetry; this.healthReporter=new CompanySecurityHealthReporter({ telemetry }); } record(metric, meta={}){ this.telemetry.increment(metric); this.diagnostics?.record?.(`companySecurity.${metric}`, meta); return this.telemetry.snapshot(); } health(){ return this.healthReporter.status(); }}
