import { createOpaqueId } from '../../../../../../packages/shared-types/src/index.js';
import { DOMAIN_SCHEMA_VERSION, CorporateActionType, assertNoInvestmentAnalysis, assertRequired } from '../contracts/index.js';
export class CorporateActionValidator { validate(ca){ assertRequired(ca,['corporateActionId','corporateActionType','effectiveDate','sourceReference'],'corporateAction'); if(!Object.values(CorporateActionType).includes(ca.corporateActionType)) throw new Error(`Invalid corporate action type: ${ca.corporateActionType}`); for (const forbidden of ['priceAdjustment','returnCalculation','dividendYield','totalReturn']) if (ca[forbidden] !== undefined) throw new Error(`Forbidden corporate-action calculation field: ${forbidden}`); assertNoInvestmentAnalysis(ca,'Corporate action metadata'); return true; }}
export class CorporateActionRegistry { constructor(){ this.items=[]; this.validator=new CorporateActionValidator(); } record(input){ const ca=Object.freeze({ corporateActionId:createOpaqueId('CACT'), domainSchemaVersion:DOMAIN_SCHEMA_VERSION, createdAt:new Date().toISOString(), ...input }); this.validator.validate(ca); this.items.push(ca); return ca; } byEntity({ companyId, securityId, listingId }){ return Object.freeze(this.items.filter(c=>c.companyId===companyId||c.securityId===securityId||c.listingId===listingId)); }}
export class CorporateActionLinker { link(input){ return Object.freeze({ ...input, linkedAt:new Date().toISOString() }); }}
export const CorporateActionMetadata = Object.freeze({});
