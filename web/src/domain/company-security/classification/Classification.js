import { createOpaqueId } from '../../../../../../packages/shared-types/src/index.js';
import { DOMAIN_SCHEMA_VERSION, ClassificationScheme, assertNoInvestmentAnalysis } from '../contracts/index.js';
export class ClassificationSchemeRegistry { constructor(){ this.schemes=new Set(); } register(scheme){ if(!Object.values(ClassificationScheme).includes(scheme)) throw new Error(`Invalid classification scheme: ${scheme}`); this.schemes.add(scheme); return scheme; }}
export class ClassificationRegistry { constructor(){ this.assignments=[]; this.schemeRegistry=new ClassificationSchemeRegistry(); } assign(input){ this.schemeRegistry.register(input.scheme); assertNoInvestmentAnalysis(input,'Classification'); const overlap=this.assignments.some(a=>a.entityType===input.entityType&&a.entityId===input.entityId&&a.scheme===input.scheme&&!a.effectiveTo&&!input.effectiveTo); if(overlap) throw new Error('Overlapping active classification'); const a=Object.freeze({ assignmentId:createOpaqueId('CLAS'), domainSchemaVersion:DOMAIN_SCHEMA_VERSION, effectiveFrom:new Date().toISOString(), effectiveTo:null, ...input }); this.assignments.push(a); return a; } byEntity(entityType,entityId){ return Object.freeze(this.assignments.filter(a=>a.entityType===entityType&&a.entityId===entityId)); }}
export const Classification = Object.freeze({}); export const ClassificationAssignment = Object.freeze({});
