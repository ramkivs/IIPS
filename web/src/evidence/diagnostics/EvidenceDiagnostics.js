export class EvidenceTelemetry { constructor(){ this.metrics={ evidenceRecordsCreated:0, evidenceSubmittedForReview:0, evidenceApproved:0, evidenceRejected:0, evidenceCertified:0, evidenceVersionsCreated:0, evidenceSnapshotsCreated:0, evidenceTraceLinksCreated:0, forbiddenActionAttempts:0 }; } increment(metric){ const m=normalize(metric); if(!(m in this.metrics)) throw new Error(`Unknown evidence metric: ${metric}`); this.metrics[m]+=1; return this.snapshot(); } snapshot(){ return Object.freeze({ ...this.metrics }); }}
export class EvidenceHealthReporter { constructor({ telemetry }){ this.telemetry=telemetry; } status(){ const m=this.telemetry.snapshot(); return Object.freeze({ status:m.forbiddenActionAttempts>0?'Degraded':'Healthy', metrics:m }); }}
export class EvidenceDiagnostics { constructor({ diagnostics, telemetry=new EvidenceTelemetry() }={}){ this.diagnostics=diagnostics; this.telemetry=telemetry; this.healthReporter=new EvidenceHealthReporter({ telemetry }); } record(metric, meta={}){ const m=normalize(metric); this.telemetry.increment(m); this.diagnostics?.record?.(`evidence.${m}`, meta); return this.telemetry.snapshot(); } health(){ return this.healthReporter.status(); }}
function normalize(metric){ return ({ 'evidence.record.created':'evidenceRecordsCreated', 'evidence.certified':'evidenceCertified', 'evidence.trace.linked':'evidenceTraceLinksCreated' })[metric] || metric; }
