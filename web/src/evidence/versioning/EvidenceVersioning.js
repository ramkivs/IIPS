export class EvidenceVersionPolicy { assertCorrection(reason){ if(!reason) throw new Error('Correction reason is required'); return true; } }
export class EvidenceCorrectionService { constructor({ registry, policy=new EvidenceVersionPolicy() }){ this.registry=registry; this.policy=policy; } correct(id,{ correctionReason, patch }){ this.policy.assertCorrection(correctionReason); return this.registry.createVersion(id,{ correctionReason, patch }); }}
export class EvidenceVersionDiff { diff(a,b){ const changes=[]; for(const k of Object.keys({ ...a, ...b })) if(JSON.stringify(a[k])!==JSON.stringify(b[k])) changes.push(k); return Object.freeze({ from:a.evidenceVersionId, to:b.evidenceVersionId, changedFields:Object.freeze(changes.filter(k=>!['contentHash'].includes(k))) }); }}
export class EvidenceImmutableStore { constructor(){ this.records=new Map(); } append(record){ const list=this.records.get(record.evidenceRecordId)||[]; list.push(Object.freeze({ ...record })); this.records.set(record.evidenceRecordId,list); return record; } byRecord(id){ return Object.freeze((this.records.get(id)||[]).slice()); }}
