export class ValuationTelemetry { constructor(){ this.metrics={ valuationModelsRegistered:0, valuationModelsActivated:0, configurationsRegistered:0, inputPackagesCreated:0, dryRunsExecuted:0, validationsExecuted:0, replaysExecuted:0, snapshotsCreated:0, forbiddenActionAttempts:0 }; } increment(metric){ if(!(metric in this.metrics)) throw new Error(`Unknown valuation metric: ${metric}`); this.metrics[metric]+=1; return this.snapshot(); } snapshot(){ return Object.freeze({ ...this.metrics }); }}
export class ValuationHealthReporter { constructor({ telemetry }){ this.telemetry=telemetry; } status(){ const m=this.telemetry.snapshot(); return Object.freeze({ status:m.forbiddenActionAttempts?'Degraded':'Healthy', metrics:m }); }}
export class ValuationDiagnostics { constructor({ diagnostics, telemetry=new ValuationTelemetry() }={}){ this.diagnostics=diagnostics; this.telemetry=telemetry; this.healthReporter=new ValuationHealthReporter({ telemetry }); } record(metric, meta={}){ this.telemetry.increment(metric); this.diagnostics?.record?.(`valuationFramework.${metric}`, meta); return this.telemetry.snapshot(); } health(){ return this.healthReporter.status(); }}
