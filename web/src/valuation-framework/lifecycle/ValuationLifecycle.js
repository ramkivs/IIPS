import { ValuationLifecycleState } from '../contracts/index.js';
const allowed={ Draft:['Registered','Failed'], Registered:['Validated','Failed'], Validated:['Active','Failed'], Active:['Deprecated','Retired','Failed'], Deprecated:['Retired'], Retired:[], Failed:[] };
export class ValuationLifecyclePolicy { canExecute(state){ return state!==ValuationLifecycleState.Retired && state!==ValuationLifecycleState.Failed; } canReplay(state){ return [ValuationLifecycleState.Active,ValuationLifecycleState.Deprecated,ValuationLifecycleState.Retired].includes(state); } }
export class ValuationLifecycleManager { constructor(){ this.states=new Map(); this.policy=new ValuationLifecyclePolicy(); } initialize(id){ this.states.set(id,ValuationLifecycleState.Draft); return this.status(id); } transition(id,to){ const from=this.states.get(id); if(!from) throw new Error(`Unknown valuation lifecycle: ${id}`); if(!(allowed[from]||[]).includes(to)){ this.states.set(id,ValuationLifecycleState.Failed); throw new Error(`Invalid valuation transition: ${from} -> ${to}`); } this.states.set(id,to); return this.status(id); } status(id){ return Object.freeze({ valuationModelId:id, state:this.states.get(id)||null }); } assertExecutable(id){ if(!this.policy.canExecute(this.states.get(id))) throw new Error('Valuation model cannot execute'); return true; }}
export const ValuationLifecycleTransition=Object.freeze({});
