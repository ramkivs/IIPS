export class DecisionTelemetry { constructor(){ this.metrics={ decisionModelsRegistered:0, inputPackagesCreated:0, decisionPoliciesEvaluated:0, approvalPoliciesEvaluated:0, rationalesCreated:0, decisionArtifactsCreated:0, snapshotsCreated:0, replaysCompleted:0, forbiddenActionAttempts:0 }; } increment(metric){ if(!(metric in this.metrics)) throw new Error(`Unknown decision metric: ${metric}`); this.metrics[metric]+=1; return this.snapshot(); } snapshot(){ return Object.freeze({ ...this.metrics }); }}
export class DecisionHealthReporter { constructor({ telemetry }){ this.telemetry=telemetry; } status(){ const m=this.telemetry.snapshot(); return Object.freeze({ status:m.forbiddenActionAttempts?'Degraded':'Healthy', metrics:m }); }}
export class DecisionDiagnostics { constructor({ diagnostics, telemetry=new DecisionTelemetry() }={}){ this.diagnostics=diagnostics; this.telemetry=telemetry; this.healthReporter=new DecisionHealthReporter({ telemetry }); } record(metric, meta={}){ this.telemetry.increment(metric); this.diagnostics?.record?.(`decision.${metric}`, meta); return this.telemetry.snapshot(); } health(){ return this.healthReporter.status(); }}
