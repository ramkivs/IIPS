export class DecisionReplayAdapter { replay(snapshot){ const m=snapshot.inputs.decisionManifest; return Object.freeze({ decisionArtifactId:m.decisionArtifactId, decisionModelId:m.decisionModelId, decisionModelVersion:m.decisionModelVersion, decisionDisposition:m.decisionDisposition, approvalDisposition:m.approvalDisposition, rationale:m.rationale, confidence:m.confidence, provenance:m.provenance }); }}
