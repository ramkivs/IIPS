import { DecisionModelManifestValidator } from '../models/index.js';
export class DecisionModelRegistry { constructor(){ this.items=new Map(); this.validator=new DecisionModelManifestValidator(); this.active=new Set(); } key(id,v){ return `${id}@${v}`; } register(m){ this.validator.validate(m); const key=this.key(m.decisionModelId,m.decisionModelVersion); if(this.items.has(key)) throw new Error(`Decision model exists: ${key}`); this.items.set(key,m); return m; } activate(id,v){ const m=this.get(id,v); this.active.add(this.key(id,v)); return m; } update(id,v,patch){ if(this.active.has(this.key(id,v))) throw new Error('Active decision model is immutable'); const next=Object.freeze({ ...this.get(id,v), ...patch }); this.validator.validate(next); this.items.set(this.key(id,v),next); return next; } get(id,v){ const m=this.items.get(this.key(id,v)); if(!m) throw new Error(`Decision model not found: ${id}@${v}`); return m; }}
export class DecisionModelVersionRegistry { constructor(){ this.versions=new Map(); } add(m){ const list=this.versions.get(m.decisionModelId)||[]; list.push(m.decisionModelVersion); this.versions.set(m.decisionModelId,Object.freeze([...new Set(list)])); return this.versions.get(m.decisionModelId); }}
