import { createOpaqueId } from '../../../../../packages/shared-types/src/index.js';
import { assertNoExecutionLogic } from '../contracts/index.js';
export class DecisionConfigurationRegistry { constructor(){ this.items=new Map(); } register({ decisionModelId, decisionModelVersion, configurationVersion='1.0.0', policySettings={} }){ const c=deepFreeze({ decisionConfigurationId:createOpaqueId('DCFG'), decisionModelId, decisionModelVersion, configurationVersion, policySettings, createdAt:new Date().toISOString() }); for(const f of ['orderGeneration','tradeExecution','portfolioConstruction','capitalDeployment','brokerIntegration','rebalancing']) if(c.policySettings?.[f]!==undefined) throw new Error(`Forbidden decision configuration field: ${f}`); assertNoExecutionLogic(c,'decision configuration'); const key=`${decisionModelId}@${decisionModelVersion}:${configurationVersion}`; if(this.items.has(key)) throw new Error(`Decision configuration exists: ${key}`); this.items.set(key,c); return c; }}
export const DecisionConfiguration=Object.freeze({}); export const DecisionConfigurationValidator=Object.freeze({});
function deepFreeze(o){ Object.freeze(o); for(const v of Object.values(o)) if(v&&typeof v==='object'&&!Object.isFrozen(v)) deepFreeze(v); return o; }
