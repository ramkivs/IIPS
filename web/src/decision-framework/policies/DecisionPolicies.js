import { createOpaqueId } from '../../../../../packages/shared-types/src/index.js';
import { DecisionDisposition, assertRequired, assertNoExecutionLogic } from '../contracts/index.js';
export class DecisionPolicyValidator { validate(p){ assertRequired(p,['decisionPolicyId','decisionPolicyVersion','policyName','inputType','allowedDispositions','provenance','evaluationRules'],'decisionPolicy'); for(const d of p.allowedDispositions) if(!Object.values(DecisionDisposition).includes(d)) throw new Error(`Invalid decision disposition: ${d}`); assertNoExecutionLogic(p,'decision policy'); return true; }}
export class DecisionPolicyRegistry { constructor(){ this.items=new Map(); this.validator=new DecisionPolicyValidator(); } register(input){ const p=Object.freeze({ decisionPolicyId:createOpaqueId('DPOL'), ...input }); this.validator.validate(p); this.items.set(`${p.decisionPolicyId}@${p.decisionPolicyVersion}`,p); return p; }}
export class DecisionPolicyEvaluator { evaluate({ policy, inputPackage }){ const direction=inputPackage.recommendationInput.recommendationDirection; let disposition=DecisionDisposition.insufficientInformation; if(direction==='constructive') disposition=DecisionDisposition.proceedToReview; else if(direction==='neutral') disposition=DecisionDisposition.pause; else if(direction==='cautious'||direction==='avoid') disposition=DecisionDisposition.returnForRevision; else disposition=DecisionDisposition.insufficientInformation; if(!policy.allowedDispositions.includes(disposition)) disposition=DecisionDisposition.insufficientInformation; return Object.freeze({ decisionPolicyId:policy.decisionPolicyId, decisionPolicyVersion:policy.decisionPolicyVersion, decisionDisposition:disposition, evaluatedAt:new Date().toISOString(), inputs:{ recommendationArtifactId:inputPackage.recommendationInput.recommendationArtifactId } }); }}
export const DecisionPolicy=Object.freeze({});
