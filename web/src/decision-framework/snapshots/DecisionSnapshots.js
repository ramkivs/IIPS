import { SnapshotEngine } from '../../../../../packages/snapshot-engine/src/index.js';
import { DECISION_SCHEMA_VERSION, assertNoExecutionLogic } from '../contracts/index.js';
export class DecisionSnapshotManifestBuilder { build({ artifact, auditTrail=[] }){ const m=Object.freeze({ decisionArtifactId:artifact.decisionArtifactId, decisionSchemaVersion:artifact.decisionSchemaVersion, decisionModelId:artifact.decisionModelId, decisionModelVersion:artifact.decisionModelVersion, inputPackageId:artifact.inputPackageId, decisionDisposition:artifact.decisionDisposition, approvalDisposition:artifact.approvalDisposition, rationale:artifact.rationale, confidence:artifact.confidence, provenance:artifact.provenance, auditTrail:Object.freeze(auditTrail.slice()), platformVersion:'2.0.0', snapshotSchemaVersion:DECISION_SCHEMA_VERSION, createdAt:new Date().toISOString() }); assertNoExecutionLogic(m,'decision snapshot'); return m; }}
export class DecisionSnapshotAdapter { constructor({ snapshotStore }={}){ this.snapshotStore=snapshotStore; this.builder=new DecisionSnapshotManifestBuilder(); } create(input){ const decisionManifest=this.builder.build(input); const snapshot=new SnapshotEngine().createSnapshot({ subject:{ type:'decision-artifact', id:decisionManifest.decisionArtifactId }, methodology:{ methodology_id:decisionManifest.decisionModelId, version:decisionManifest.decisionModelVersion }, data_version:DECISION_SCHEMA_VERSION, platform_version:'2.0.0', inputs:{ decisionManifest }, outputs:{ decisionDisposition:decisionManifest.decisionDisposition, approvalDisposition:decisionManifest.approvalDisposition } }); this.snapshotStore?.append?.(snapshot); return Object.freeze({ decisionManifest, snapshot }); }}
