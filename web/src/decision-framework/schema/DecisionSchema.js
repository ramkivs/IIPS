import { createOpaqueId } from '../../../../../packages/shared-types/src/index.js';
import { DECISION_SCHEMA_VERSION, DecisionArtifactStatus, DecisionDisposition, ApprovalDisposition, assertRequired, assertNoExecutionLogic } from '../contracts/index.js';
export class DecisionArtifactValidator { validate(a){ assertRequired(a,['decisionArtifactId','decisionSchemaVersion','decisionModelId','decisionModelVersion','inputPackageId','decisionDisposition','approvalDisposition','rationale','confidence','provenance','status','createdAt','extensions'],'decisionArtifact'); if(!Object.values(DecisionDisposition).includes(a.decisionDisposition)) throw new Error(`Invalid decision disposition: ${a.decisionDisposition}`); if(!Object.values(ApprovalDisposition).includes(a.approvalDisposition)) throw new Error(`Invalid approval disposition: ${a.approvalDisposition}`); if(!Object.values(DecisionArtifactStatus).includes(a.status)) throw new Error(`Invalid decision artifact status: ${a.status}`); assertNoExecutionLogic(a,'decision artifact'); return true; }}
export function createDecisionArtifact(input={}){ assertNoExecutionLogic(input,'decision artifact input'); const { decisionModelId, decisionModelVersion, inputPackageId, decisionDisposition, approvalDisposition, rationale, confidence={}, provenance={}, auditReference=null, status=DecisionArtifactStatus.Created, extensions={} }=input; const artifact=deepFreeze({ decisionArtifactId:createOpaqueId('DART'), decisionSchemaVersion:DECISION_SCHEMA_VERSION, decisionModelId, decisionModelVersion, inputPackageId, decisionDisposition, approvalDisposition, rationale, confidence:Object.freeze({ ...confidence }), provenance:Object.freeze({ ...provenance }), auditReference, status, createdAt:new Date().toISOString(), extensions:Object.freeze({ ...extensions }) }); new DecisionArtifactValidator().validate(artifact); return artifact; }
export const DecisionArtifactSchema=Object.freeze({ version:DECISION_SCHEMA_VERSION });
function deepFreeze(o){ Object.freeze(o); for(const v of Object.values(o)) if(v&&typeof v==='object'&&!Object.isFrozen(v)) deepFreeze(v); return o; }
