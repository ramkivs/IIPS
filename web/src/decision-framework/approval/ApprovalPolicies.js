import { createOpaqueId } from '../../../../../packages/shared-types/src/index.js';
import { ApprovalDisposition, assertRequired, assertNoExecutionLogic } from '../contracts/index.js';
export class ApprovalPolicyValidator { validate(p){ assertRequired(p,['approvalPolicyId','approvalPolicyVersion','policyName','inputType','allowedApprovalDispositions','provenance','evaluationRules'],'approvalPolicy'); for(const d of p.allowedApprovalDispositions) if(!Object.values(ApprovalDisposition).includes(d)) throw new Error(`Invalid approval disposition: ${d}`); assertNoExecutionLogic(p,'approval policy'); return true; }}
export class ApprovalPolicyRegistry { constructor(){ this.items=new Map(); this.validator=new ApprovalPolicyValidator(); } register(input){ const p=Object.freeze({ approvalPolicyId:createOpaqueId('APOL'), ...input }); this.validator.validate(p); this.items.set(`${p.approvalPolicyId}@${p.approvalPolicyVersion}`,p); return p; }}
export class ApprovalPolicyEvaluator { evaluate({ policy, decisionEvaluation }){ const outcome=decisionEvaluation.decisionDisposition==='proceed-to-review'?ApprovalDisposition.approvalRequired:ApprovalDisposition.approvalNotRequired; const final=policy.allowedApprovalDispositions.includes(outcome)?outcome:ApprovalDisposition.approvalDeferred; return Object.freeze({ approvalEvaluationId:createOpaqueId('AEVAL'), approvalPolicyId:policy.approvalPolicyId, approvalPolicyVersion:policy.approvalPolicyVersion, evaluationTimestamp:new Date().toISOString(), evaluationOutcome:final }); }}
export const ApprovalPolicy=Object.freeze({});
