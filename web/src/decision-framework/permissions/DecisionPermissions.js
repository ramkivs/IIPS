import { DecisionPermissionAction } from '../contracts/index.js';
const forbidden=new Set(['optimizePortfolio','generateOrder','placeOrder','executeTrade','deployCapital','integrateBroker','rebalancePortfolio']);
export class DecisionPermissionRegistry { constructor(){ this.permissions=new Map(); } register({ action, effect='allow' }){ if(forbidden.has(action)) throw new Error(`Forbidden decision permission: ${action}`); if(!Object.values(DecisionPermissionAction).includes(action)) throw new Error(`Unsupported decision permission: ${action}`); if(this.permissions.has(action)) throw new Error(`Permission exists: ${action}`); const p=Object.freeze({ action, effect }); this.permissions.set(action,p); return p; } get(action){ return this.permissions.get(action)||null; }}
export class DecisionPermissionGate { constructor({ registry }){ this.registry=registry; } require(action){ const p=this.registry.get(action); if(!p||p.effect!=='allow') throw new Error(`Decision permission denied: ${action}`); return true; }}
export function registerDefaultDecisionPermissions(registry){ for(const action of Object.values(DecisionPermissionAction)) if(!registry.get(action)) registry.register({ action }); return registry; }
