import { assertNoResearchIntelligence, validateSubject } from '../contracts/index.js';
export class ResearchContextPolicy { validate(context){ validateSubject(context.subject); assertNoResearchIntelligence(context, 'Research context'); for (const forbidden of ['companyId','securityId','portfolioId','ticker','marketCap','revenue','price','valuation','score','rating','recommendation']) if (context[forbidden] !== undefined) throw new Error(`Forbidden Research context field: ${forbidden}`); return true; } }
export class ResearchContextProjection { constructor(context){ this.context=deepFreeze({ ...context }); } snapshot(){ return this.context; } }
export class ResearchContextProjector { constructor({ policy=new ResearchContextPolicy() } = {}) { this.policy=policy; }
  project(productContext, overrides={}){ const base=productContext?.snapshot ? productContext.snapshot() : productContext; this.policy.validate(base); const context={ researchModuleId:'research-module-foundation', workflowInstanceId:base.workflowInstanceId, workspaceId:base.workspaceId, subject:base.subject||null, activeDocumentId:null, selectedArtifactIds:[], visiblePanels:['documents','artifacts','context','activity'], permissions:base.permissions||[], lastUpdatedAt:new Date().toISOString(), ...overrides }; this.policy.validate(context); return new ResearchContextProjection(context); }}
function deepFreeze(obj){ Object.freeze(obj); for(const v of Object.values(obj)) if(v&&typeof v==='object'&&!Object.isFrozen(v)) deepFreeze(v); return obj; }
