export class ResearchIntelligenceTelemetry { constructor(){ this.metrics={ templatesRegistered:0, templatesApplied:0, sectionsCreated:0, notesCreated:0, reviewsStarted:0, reviewsCompleted:0, commentsAdded:0, checklistItemsCompleted:0, completenessChecksRun:0, evidenceCandidatesCreated:0, forbiddenActionAttempts:0 }; } increment(metric){ if(!(metric in this.metrics)) throw new Error(`Unknown Research Intelligence metric: ${metric}`); this.metrics[metric]+=1; return this.snapshot(); } snapshot(){ return Object.freeze({ ...this.metrics }); }}
export class ResearchIntelligenceHealthReporter { constructor({ telemetry }){ this.telemetry=telemetry; } status(){ const m=this.telemetry.snapshot(); return Object.freeze({ status:m.forbiddenActionAttempts>0?'Degraded':'Healthy', metrics:m }); }}
export class ResearchIntelligenceDiagnostics { constructor({ diagnostics, telemetry=new ResearchIntelligenceTelemetry() }={}){ this.diagnostics=diagnostics; this.telemetry=telemetry; this.healthReporter=new ResearchIntelligenceHealthReporter({ telemetry }); } record(metric, meta={}){ const normalized = normalizeMetric(metric); this.telemetry.increment(normalized); this.diagnostics?.record?.(`researchIntelligence.${normalized}`, meta); return this.telemetry.snapshot(); } health(){ return this.healthReporter.status(); }}
function normalizeMetric(metric){ return ({ 'researchIntelligence.template.registered':'templatesRegistered', 'researchIntelligence.template.applied':'templatesApplied', 'researchIntelligence.section.created':'sectionsCreated', 'researchIntelligence.note.created':'notesCreated', 'researchIntelligence.review.started':'reviewsStarted', 'researchIntelligence.comment.added':'commentsAdded', 'researchIntelligence.checklist.completed':'checklistItemsCompleted', 'researchIntelligence.evidenceCandidate.created':'evidenceCandidatesCreated' })[metric] || metric; }
