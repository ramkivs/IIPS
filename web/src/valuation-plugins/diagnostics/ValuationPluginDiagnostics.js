export class ValuationPluginTelemetry { constructor(){ this.metrics={ pluginsRegistered:0, pluginsLoaded:0, pluginsEnabled:0, pluginDryRunsExecuted:0, pluginValidationsExecuted:0, pluginSnapshotsCreated:0, pluginReplaysExecuted:0, forbiddenActionAttempts:0 }; } increment(metric){ if(!(metric in this.metrics)) throw new Error(`Unknown plugin metric: ${metric}`); this.metrics[metric]+=1; return this.snapshot(); } snapshot(){ return Object.freeze({ ...this.metrics }); }}
export class ValuationPluginHealthReporter { constructor({ telemetry }){ this.telemetry=telemetry; } status(){ const m=this.telemetry.snapshot(); return Object.freeze({ status:m.forbiddenActionAttempts?'Degraded':'Healthy', metrics:m }); }}
export class ValuationPluginDiagnostics { constructor({ diagnostics, telemetry=new ValuationPluginTelemetry() }={}){ this.diagnostics=diagnostics; this.telemetry=telemetry; this.healthReporter=new ValuationPluginHealthReporter({ telemetry }); } record(metric, meta={}){ this.telemetry.increment(metric); this.diagnostics?.record?.(`valuationPlugin.${metric}`, meta); return this.telemetry.snapshot(); } health(){ return this.healthReporter.status(); }}
