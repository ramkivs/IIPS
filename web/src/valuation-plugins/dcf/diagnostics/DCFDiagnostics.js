export class DCFTelemetry { constructor(){ this.metrics={ dcfExecutionsStarted:0, dcfExecutionsCompleted:0, dcfValidationFailures:0, dcfSnapshotsCreated:0, dcfReplaysCompleted:0, forbiddenActionAttempts:0 }; } increment(metric){ if(!(metric in this.metrics)) throw new Error(`Unknown DCF metric: ${metric}`); this.metrics[metric]+=1; return this.snapshot(); } snapshot(){ return Object.freeze({ ...this.metrics }); }}
export class DCFHealthReporter { constructor({ telemetry }){ this.telemetry=telemetry; } status(){ const m=this.telemetry.snapshot(); return Object.freeze({ status:m.dcfValidationFailures||m.forbiddenActionAttempts?'Degraded':'Healthy', metrics:m }); }}
export class DCFDiagnostics { constructor({ diagnostics, telemetry=new DCFTelemetry() }={}){ this.diagnostics=diagnostics; this.telemetry=telemetry; this.healthReporter=new DCFHealthReporter({ telemetry }); } record(metric, meta={}){ this.telemetry.increment(metric); this.diagnostics?.record?.(`dcf.${metric}`, meta); return this.telemetry.snapshot(); } health(){ return this.healthReporter.status(); }}
