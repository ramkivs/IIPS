export class RelativeValuationTelemetry { constructor(){ this.metrics={ relativeValuationExecutionsStarted:0, relativeValuationExecutionsCompleted:0, relativeValuationValidationFailures:0, relativeValuationSnapshotsCreated:0, relativeValuationReplaysCompleted:0, forbiddenActionAttempts:0 }; } increment(metric){ if(!(metric in this.metrics)) throw new Error(`Unknown relative valuation metric: ${metric}`); this.metrics[metric]+=1; return this.snapshot(); } snapshot(){ return Object.freeze({ ...this.metrics }); }}
export class RelativeValuationHealthReporter { constructor({ telemetry }){ this.telemetry=telemetry; } status(){ const m=this.telemetry.snapshot(); return Object.freeze({ status:m.relativeValuationValidationFailures||m.forbiddenActionAttempts?'Degraded':'Healthy', metrics:m }); }}
export class RelativeValuationDiagnostics { constructor({ diagnostics, telemetry=new RelativeValuationTelemetry() }={}){ this.diagnostics=diagnostics; this.telemetry=telemetry; this.healthReporter=new RelativeValuationHealthReporter({ telemetry }); } record(metric, meta={}){ this.telemetry.increment(metric); this.diagnostics?.record?.(`relativeValuation.${metric}`, meta); return this.telemetry.snapshot(); } health(){ return this.healthReporter.status(); }}
