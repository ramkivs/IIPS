import { assertNoRelativeDecisionLogic } from '../contracts/index.js';
class BaseProjection { constructor({ projectionId, supportedEvents }){ this.definition=Object.freeze({ projectionId, snapshotCompatible:true, projectionVersion:'1.0.0', supportedEvents:Object.freeze(supportedEvents) }); this.state=Object.freeze({}); } apply(event){ if(!this.definition.supportedEvents.includes(event.type)) throw new Error(`Unsupported relative valuation event: ${event.type}`); assertNoRelativeDecisionLogic(event,'Relative valuation projection'); this.state=deepFreeze({ ...this.state, lastEventType:event.type, payload:event.payload||{}, updatedAt:new Date().toISOString() }); return this.state; } snapshot(){ return deepFreeze({ definition:this.definition, state:this.state }); }}
export class RelativeValuationExecutionProjection extends BaseProjection { constructor(){ super({ projectionId:'relative.execution', supportedEvents:['RelativeValuationExecutionCompleted'] }); }}
export class RelativeValuationArtifactProjection extends BaseProjection { constructor(){ super({ projectionId:'relative.artifact', supportedEvents:['RelativeValuationResultCreated'] }); }}
export class RelativeValuationAuditProjection extends BaseProjection { constructor(){ super({ projectionId:'relative.audit', supportedEvents:['RelativeValuationAuditRecorded'] }); }}
export class RelativeValuationProjectionRegistry { constructor(){ this.projections=new Map(); } register(p){ this.projections.set(p.definition.projectionId,p); return p; } registerDefaults(){ [new RelativeValuationExecutionProjection(),new RelativeValuationArtifactProjection(),new RelativeValuationAuditProjection()].forEach(p=>this.register(p)); return this; } get(id){ const p=this.projections.get(id); if(!p) throw new Error(`Projection not found: ${id}`); return p; } update(id,event){ return this.get(id).apply(event); }}
function deepFreeze(o){ Object.freeze(o); for(const v of Object.values(o)) if(v&&typeof v==='object'&&!Object.isFrozen(v)) deepFreeze(v); return o; }
