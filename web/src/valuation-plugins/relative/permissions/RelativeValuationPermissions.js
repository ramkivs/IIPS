import { RelativeValuationPermissionAction } from '../contracts/index.js';
const forbidden=new Set(['recommendInvestment','assignRating','scoreCompany','rankPeer','screenPeer','approveInvestment','rebalancePortfolio']);
export class RelativeValuationPermissionRegistry { constructor(){ this.permissions=new Map(); } register({ action, effect='allow' }){ if(forbidden.has(action)) throw new Error(`Forbidden relative valuation permission: ${action}`); if(!Object.values(RelativeValuationPermissionAction).includes(action)) throw new Error(`Unsupported relative valuation permission: ${action}`); if(this.permissions.has(action)) throw new Error(`Permission exists: ${action}`); const p=Object.freeze({ action, effect }); this.permissions.set(action,p); return p; } get(action){ return this.permissions.get(action)||null; }}
export class RelativeValuationPermissionGate { constructor({ registry }){ this.registry=registry; } require(action){ const p=this.registry.get(action); if(!p||p.effect!=='allow') throw new Error(`Relative valuation permission denied: ${action}`); return true; }}
export function registerDefaultRelativeValuationPermissions(registry){ for(const action of Object.values(RelativeValuationPermissionAction)) if(!registry.get(action)) registry.register({ action }); return registry; }
